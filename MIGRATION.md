# Migration from Salesmax

Not started (Phase 2). Depends on the Phase 1 ingestion gate, because imports must pass through it (MG-3 re-runnability comes from the gate's idempotency key = Salesmax lead id).

Needed from G-TEC before Phase 2: a sample Salesmax export of Active and Worked Leads with all columns, confirmation whether call/note history is exportable, and the mapping sheet (17 users, 14 teams, 69 pages, 89 lists → centre codes).
